"""The Countdown.

Fill in the body. Run costs nothing -- it happens in your browser. Submit sends it to the
server, which has tests you cannot read (spec §6.3).

A plain script, no `def`. Orders one under the other, the way every Area 1 file is written.
"""

import turtle

# 1. Start with a length of 200.
# 2. While the length is still MORE THAN 20:
#       draw a line that long, turn right 90, and make the length 20 shorter.
# 3. Nothing counts the lines. The condition decides when to stop.

turtle.done()
