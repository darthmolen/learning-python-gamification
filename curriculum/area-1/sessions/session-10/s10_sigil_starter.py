"""The Sigil — starter file.

This file runs. That is the only thing it does.

Read sigil-brief.md, pick one of the three framings, delete this docstring and
build it. You may delete anything in here. Nothing below is a hint.

Run:  py -3.14 s10_sigil_starter.py
"""
# concepts: for, while, range, if, elif, else, comparison-operators, boolean-operators, nesting, accumulator-pattern, print, variables, int, float, str, bool, input, f-strings, reading-errors
# dc: 20
# expect: runs

import turtle


# --- THE ORDERS YOU OWN -------------------------------------------------------
# Movement       forward(n)  backward(n)  left(deg)  right(deg)  goto(x, y)
#                home()  setheading(deg)  circle(r)
# The pen        penup()  pendown()  pensize(n)  color("name")
# Speed          speed(0)          ...and, if you need it:
#                tracer(0) near the top, update() just before done()
# Asking         position()  heading()  isdown()
# Text           write(text, False, "center", ("Arial", 20, "bold"))
# Python         for x in range(a, b, c):      while question:
#                if / elif / else       and  or  not
#                >  <  >=  <=  ==  !=
#                total = total + n      print(...)  input("...")
#                int(...)  float(...)  str(...)  type(...)
#                f"...{...}..."
#
# The rule you have used in every file since session 1, because you will want it
# tonight and it is the only one nobody can look up for you:
#
#     the turns of a closed shape add up to 360
# -----------------------------------------------------------------------------


print("Sigil accepted. Nothing drawn yet.")


turtle.done()
