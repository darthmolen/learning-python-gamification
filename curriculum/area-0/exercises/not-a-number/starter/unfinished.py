"""Not A Number — int() turns text into a number. Usually.

Broken sigil 6, from session 3. The order is spelled right, the brackets are
closed, the indentation is fine, and it still falls over.

The size is meant to be ten.

Run:  py -3.14 solution.py
"""

import turtle

size = int("ten")
turtle.forward(size)


# --- YOUR MOVE ---------------------------------------------------------------
# 1. Run it. Write down the error's NAME and its LINE NUMBER.
#
# 2. This error is about a VALUE, not a name and not a kind. int() was given
#    something it was perfectly willing to look at and could not read. Say
#    what int() would have accepted instead.
#
# 3. Fix it so the turtle walks ten steps. FIX the line — do not delete it,
#    and do not delete the order underneath it either.
# -----------------------------------------------------------------------------


turtle.done()
