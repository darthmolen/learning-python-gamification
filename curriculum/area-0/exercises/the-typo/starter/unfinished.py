"""The Typo — read the error before you fix it.

You met this one in session 3. It is broken on purpose and the fix is one
character, which is not the point. The point is what Python says on the way
down: the NAME of the error and the LINE it happened on.

Run it first. Read what falls out. Then fix it.

Run:  py -3.14 solution.py
"""

import turtle

turtle.forward(100)
turtel.left(90)
turtle.forward(100)


# --- YOUR MOVE ---------------------------------------------------------------
# 1. Run it. Before you touch anything, write down the error's NAME and the
#    LINE NUMBER it happened on.
#
# 2. Fix that line. FIX it — do not delete it. Deleting it makes the error go
#    away and leaves you with one long straight line, which is not this
#    program. The turtle still has to turn its corner.
# -----------------------------------------------------------------------------


turtle.done()
