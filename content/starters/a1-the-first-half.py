"""The First Half.

Fill in the body. Run costs nothing -- it happens in your browser. Submit sends it to the
server, which has tests you cannot read (spec §6.3).

A plain script, no `def`. Orders one under the other, the way every Area 1 file is written.
"""

import turtle

# 1. Draw a 12-sided shape. Every side 60 long, and work the turn out from the 12.
# 2. Before drawing each side, ask which half it is in:
#       the first six sides are "red", the rest are "black".
# 3. Watch your indentation. Three levels now, and each one means something.

turtle.done()
