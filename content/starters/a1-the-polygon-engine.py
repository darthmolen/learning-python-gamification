"""The Polygon Engine.

Fill in the body. Run costs nothing -- it happens in your browser. Submit sends it to the
server, which has tests you cannot read (spec §6.3).

This is a plain script with no `def` in it, because `def` is Area 4 and you have not met
it yet. Write your orders one under the other, exactly like every file in Area 1.
"""

import turtle

# 1. Ask how many sides, and turn the answer into a number.
# 2. Work the turn out from the number of sides. Do not type it.
# 3. Draw the shape: every side 100 long.

turtle.done()
