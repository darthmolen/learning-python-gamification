"""The Gatekeeper.

Fill in the body. Run costs nothing -- it happens in your browser. Submit sends it to the
server, which has tests you cannot read (spec §6.3).

A plain script, no `def`. Orders one under the other, the way every Area 1 file is written.
"""

import turtle

# 1. Ask "How big?" and turn the answer into a whole number.
# 2. A ladder, in this order:
#       below 20 or above 300  -> print a line with the word REFUSED in it, draw nothing
#       20 to 100              -> pen size 3
#       anything else          -> pen size 8
# 3. If it was not refused, draw a square with sides that long.

turtle.done()
