# Session 3's broken sigils, promoted to fix-it quests. b2 — TypeError, a number in quotation marks.
#
# The verifier has two halves and needs both (curriculum/area-0/README.md): the hidden
# test proves the error is gone, and the turtle spy proves the behaviour is still there.
# A quest that only checked "did it run" would accept deleting the broken line, which is
# the one answer session 3 exists to rule out.
id: a0-the-wrong-kind
title: "The Wrong Kind Of Thing"
kind: quest
area: 0
concepts: [reading-errors, str, int]
dc: 8                # XP and the risk label both derive from this (§5.1)
brief: briefs/a0-the-wrong-kind.md

verifier:
  type: hidden-tests
  starter: starters/a0-the-wrong-kind.py
  tests: tests/a0-the-wrong-kind_test.py
