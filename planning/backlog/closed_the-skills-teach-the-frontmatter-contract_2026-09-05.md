---
kind: stub
status: closed
date: 2026-09-05
promoted_from: plans-declare-their-status-in-frontmatter_2026-09-04
closed_reason: all three files shipped 2026-09-06 in c:\dev\ai-plugins-and-skills; no plan was needed
---

# The Skills Teach The Frontmatter Contract

**Closed 2026-09-06.** All three files shipped. No plan was written for it — the contract was
already settled and enforced by `validate:plans`, so this was a page of writing rather than a
design, which is exactly the shape a stub is allowed to close as.

## Context

`plans-declare-their-status-in-frontmatter_2026-09-04` migrated all 113 planning documents to
frontmatter and shipped `npm run validate:plans` to check them. The half of its Phase 4 that lives
in **this** repository is done — `CLAUDE.md` describes the board, the contract and
references-as-names.

The other half is in `c:\dev\ai-plugins-and-skills`, which has its own history and its own
`index.json`, and it was always going to be a second commit.

**So today an agent writing a new planning document is not told to write frontmatter.** It finds
out when `validate:plans` refuses the document. That is the safety net working exactly as designed,
and it is still the worse of the two orders: being told beforehand beats being caught afterwards,
and the corpus is now uniform enough that a skill can describe it in one page.

## Known Scope

Three files, all in `c:\dev\ai-plugins-and-skills`. **Two are done as of 2026-09-05:**

- ✅ **`skills/plan-writing-syntax/SKILL.md`** — written 2026-09-05. `category: workflow-composers`
  with no `order`, matching `plan-workflow` and the other board skills; `review-helpers` carries an
  order and is the review pipeline only. The split is the one that already existed between a board
  and a form: `plan-workflow` says which column a card is in, this says what is written on it.
- ✅ **`index.json`** — regenerated by `scripts/build_index.py`. 39 skills, 6 categories. The
  generator also rewrites the README table and `documentation/ARCHITECTURE.md`'s tree; the
  hand-written category blurb above the table was extended by hand to name the new skill, and
  survives regeneration.
- ✅ **`skills/plan-workflow/SKILL.md`** — done 2026-09-06. All ten directories, with
  `not-implemented/` given its own section arguing why both alternatives lie. "The path tells you
  the status" is replaced by **the directory and the `status:` field must agree, so a move is two
  edits** — the drift the field exists to catch. The decision tree gained the two terminal moves it
  was missing, the stub template carries frontmatter, and the deleted-stub incident is written into
  the guardrail it broke, because "never delete plan documents" had plainly not been vivid enough.

The authority for all of it is `pyquest/scripts/plans.ts`, which is the executable version of the
contract. **Write the skill from that file rather than from the parent plan's prose** — the plan
described the schema before it was built, and three things moved during implementation: `reminder`
gained `dropped`, a closed reminder may live in either reminders directory, and a reference never
resolves to the document making it.

## Trigger for Promotion

None — this closed rather than being promoted. Nothing in it needed a plan: the contract was
already settled and already enforced, so the whole item was a page of writing against it.
